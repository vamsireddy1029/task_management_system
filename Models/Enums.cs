namespace TaskManagementApi.Models
{
    public enum TaskItemStatus
    {
        TODO,
        IN_PROGRESS,
        DONE
    }

    public enum TaskPriority
    {
        LOW,
        MEDIUM,
        HIGH
    }
}